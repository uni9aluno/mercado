// Stub temporário — cada tela real substitui isto na Fase 4.
export function Placeholder({ nome }: { nome: string }) {
  return (
    <div className="fade-in p-6 text-center text-slate-400">
      <p className="text-lg font-medium text-slate-500">{nome}</p>
      <p className="mt-1 text-sm">Tela em construção (Fase 4).</p>
    </div>
  );
}
